# Remonter machine Windows 
Dans service active le "service SNMP" si il y est pas l'ajouter dans les service de l'AD
Le parametrer dans sécurité mettre le nom de communautée "EyesOfNetwork" et mettre l'ip de l'host 172.50.6.70
redemarer le service SNMP 

# Remonter machine Linux 
apt-get install snmpd snmp
systemctl enable snmpd
systemctl status snmpd

https://wiki.mdl29.net/lib/exe/fetch.php?media=braveo:asterisk_linksys_spa_de_a_a_z.pdf
ssh 192.168.4.239 -p 8020 -l admin-lb
